labels:
  ok:  "resp.status == 200"
  err:  "resp.status != 200"

verify:
  in: true
  out: true

routes:

  /api/v1/users:
    get:
      params: params.json # this defines parameters
      ok: output.json

    post:
      in: Person.json
      ok: RetVal.json
      err: RetVal.json
